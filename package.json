{
  "name": "perm-history-timeline",
  "version": "1.0.0",
  "description": "Интерактивная лента времени истории Перми",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000 --host=localhost",
    "dev": "live-server --port=3000 --host=localhost",
    "build": "node build.js",
    "clean": "if exist dist rmdir /s /q dist",
    "zip": "npm run build && cd dist && powershell Compress-Archive -Path * -DestinationPath ../perm-history-site.zip -Force && echo 📦 Создан архив perm-history-site.zip",
    "deploy": "npm run build && echo 🚀 Готово! Загрузите папку dist на хостинг"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  },
  "keywords": ["Пермь", "история", "лента времени"],
  "author": "",
  "license": "ISC"
}
